{
  "name": "hone-embed",
  "version": "0.1.5",
  "description": "A event emitter for embeded apps that allows you to talk to it via events rather then onMessage",
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js -o build/hone-embed.js && npm run compress",
    "compress": "uglifyjs --screw-ie8 -o build/hone-embed.min.js build/hone-embed.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "postMessage"
  ],
  "company": "Hone Inc (http://gohone.com)",
  "author": "Jacob Lowe",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^8.1.1",
    "uglify-js": "^2.4.16"
  },
  "dependencies": {
    "eventemitter2": "^0.4.14",
    "post-emitter": "^0.1.0"
  }
}
